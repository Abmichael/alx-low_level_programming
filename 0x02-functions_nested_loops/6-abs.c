#include "main.h"
/**
 * _abs - return absolute value
 *
 *@n: The int to calculate abs
 * Return: absolute value of n.
 */

int _abs(int n)
{
	if (n < 0)
		return (n * (-1));
	else if (n == 0)
		return (0);
	else
		return ();
}
